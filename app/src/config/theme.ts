/** Re-export from the single app config. See appConfig.ts for the full source. */
export {
  colors,
  lightTheme,
  darkTheme,
  spacing,
  radius,
  fontSize,
  fontWeight,
  statusColor,
} from './appConfig';
export type {Theme} from './appConfig';
